<!-- Документ объясняет устройство и изменение прототипа. -->
# CLAUDE.md

Общие обязательные правила проекта находятся в корневых `CLAUDE.md` и `AGENTS.md`. Этот файл дополняет их инструкциями для папки `tg-app/`.

## Что это

Локальный интерактивный прототип Telegram Mini App магазина `Fashion Store` по предзаказу в Элисте.

Прототип показывает полный путь покупателя и продавца, но не подключён к серверу, платёжной системе, боту или реальным остаткам.

## Файлы

- `index.html` — точка входа, мобильная оболочка и подключение Telegram Web App SDK.
- `styles.css` — светлая и тёмная темы, mobile-first компоновка, компоненты и анимации.
- `data.js` — бренд, товары, цвета, размеры, остатки и способы получения.
- `core.js` — тестируемая бизнес-логика фильтров, корзины, итогов и заказа.
- `ui.js` — единый набор SVG-иконок интерфейса.
- `app.js` — экраны, события, Telegram SDK, BackButton и API-состояние.
- `assets/` — локальные демонстрационные фотографии и сведения об их происхождении.
- `tests/` — автоматические проверки данных и бизнес-логики.
- `docs/superpowers/` — согласованная спецификация и план реализации.

## Навигация покупателя

Пять постоянных разделов:

1. `home` — Главная.
2. `catalog` — Каталог.
3. `cart` — Корзина.
4. `orders` — Заказы.
5. `store` — Магазин.

Внутренние экраны:

- `product` — карточка товара;
- `checkout-contact` — контакты;
- `checkout-delivery` — получение;
- `checkout-review` — проверка заказа;
- `payment-success` — результат демонстрационной оплаты;
- `order-detail` — текущий заказ.

Путь покупателя:

```text
Главная → Каталог → Товар → Корзина
→ Контакты → Получение → Проверка
→ Демо-оплата → Заказ
```

## Навигация продавца

Кнопка «Режим продавца» находится внизу раздела «Магазин».

```text
Магазин → Заказы продавца → Карточка заказа
→ Подтверждение «Заказ собран»
```

Экраны продавца:

- `seller-orders` — вкладки «Собрать» и «Готовы»;
- `seller-order` — состав заказа и изменение статуса.
- `seller-users` — счётчики и список пользователей с проверенным открытием Mini App;
- `seller-user` — карточка пользователя, даты открытий и факт созданного заказа.

Вход продавца выполняется через серверную проверку raw `Telegram.WebApp.initData`, свежесть `auth_date` и Telegram ID сотрудника. Кнопка демо-доступа удалена.

## Где менять данные

Стартовые данные и утверждённые условия предзаказа находятся в `data.js`.

### Магазин

Объект `STORE` содержит название, описание, адрес, график и контакты.

### Категории

До серверной фазы `CATEGORIES` содержит только техническое значение `all`, а `PRODUCTS` пуст. Настоящие общие категории должны появиться только вместе с серверным источником каталога.

### Товар

Обязательные поля:

- `id` — уникальный латинский идентификатор;
- `name` — название товара;
- `category` — идентификатор категории;
- `price` — текущая цена числом;
- `oldPrice` — реальная старая цена или `null`;
- `badge` — «Новинка», «Последний» или `null`;
- `images` — массив локальных путей;
- `colors` — цвета с `id`, названием и HEX-кодом;
- `description`, `composition`, `care`, `fit`, `model` — данные карточки;
- `measurements` — замеры по размерам;
- `variants` — остатки конкретных сочетаний цвет + размер.

Остаток задаётся так:

```js
{ colorId: 'blue', size: 'M', stock: 2 }
```

Нулевой остаток оставляет размер видимым, но кнопка становится отключённой и приглушённой.

### Получение

Массив `DELIVERY_METHODS` содержит только самовывоз в Элисте. Поле `demo: true` нельзя убирать, пока реальный процесс оплаты и выдачи не подключён.

## Как заменить изображение

1. Подготовить вертикальный JPEG шириной около 800–1000 px.
2. Положить файл в `assets/`.
3. Использовать латинское имя в нижнем регистре без пробелов.
4. В `data.js` заменить путь в массиве `images` нужного товара.
5. Запустить автоматические тесты.

## Локальное сохранение

- `fashion-store-cart-v1` — корзина.
- `fashion-store-order-v1` — текущий демонстрационный заказ.
- `fashion-store-offer-seen-v1` — отметка о показе приветственного оффера.
- `fashion-store-preorder-reset-v1` — отметка о разовой очистке прежних демо-данных.

Чтобы снова проверить оффер первого запуска, выполните в консоли браузера:

```js
localStorage.removeItem('fashion-store-offer-seen-v1');
location.reload();
```

При первом запуске этой версии приложение удаляет старые локальные корзину, заказ и административный каталог. Чтобы повторить эту очистку при ручной проверке, удалите `fashion-store-preorder-reset-v1`, затем перезагрузите страницу.

## Запуск

Из корня проекта:

```bash
python3 -m http.server 4173 --directory tg-app
```

Затем открыть:

```text
http://localhost:4173
```

Открытие `index.html` двойным нажатием не рекомендуется: локальный сервер точнее воспроизводит размещённый Mini App.

## Тесты

Из корня проекта:

```bash
node --test tg-app/tests/*.test.js
```

Проверка синтаксиса:

```bash
node --check tg-app/data.js
node --check tg-app/core.js
node --check tg-app/app.js
```

## Telegram Web App SDK

`index.html` подключает официальный файл:

```text
https://telegram.org/js/telegram-web-app.js
```

Приложение использует:

- `ready()` и `expand()`;
- `themeParams` и событие `themeChanged`;
- `BackButton`;
- `openTelegramLink()` для шаринга ссылки на бота;
- имя из `initDataUnsafe.user` только для приветствия;
- raw `Telegram.WebApp.initData` отправляется в `user-api` для учёта открытия только внутри Telegram; `initDataUnsafe` не используется для доступа или статистики;
- safe areas через CSS-переменные Telegram и `env(safe-area-inset-*)`.

В обычном браузере используется безопасный fallback: имя «Гость», собственная кнопка назад и запасные цвета темы.

## Визуальная система

- Стиль: Editorial Minimal с крупными фотографиями и спокойными поверхностями.
- Заголовки: Cormorant; интерфейс и основной текст: Manrope с системными fallback-шрифтами.
- Иконки: единый локальный SVG-набор из `ui.js`, без эмодзи и текстовых пиктограмм.
- На 375, 768 и 1024 px интерфейс сохраняет компактную ширину Mini App до 520 px и не превращается в desktop-сайт.
- `#screen` — единственный вертикально прокручиваемый контейнер; внешняя страница не скроллится под закреплённой навигацией.
- Клавиатурный фокус показывается через `:focus-visible`; анимации учитывают `prefers-reduced-motion`.

## Ограничения прототипа

Не реализованы:

- серверные заказы и роли нескольких сотрудников;
- загрузка фотографий в Storage из редактора;
- резервирование последнего экземпляра;
- платёжный провайдер, callback и онлайн-касса;
- настоящая доставка, маршрут и возврат денег;
- уведомления Telegram-бота;
- серверное подтверждение оплаты и синхронизация заказов между устройствами.

Нельзя использовать этот прототип для приёма реальных заказов и оплаты без серверной части и проверки процессов магазина.
